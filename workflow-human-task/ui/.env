# Task queue of the integration this dashboard belongs to (matches
# `taskQueue` in ballerina/Config.toml). Items on other task queues are
# shown grayed out because no worker here serves them.
VITE_TASK_QUEUE=CLAIM_APPROVAL_QUEUE
